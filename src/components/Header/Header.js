import React from 'react';
import Logo from '../Logo';
import SearchForm from '../SearchForm';

const Header = () => (
    <header>
        <Logo />
        <SearchForm />
    </header>
);

export default Header;
